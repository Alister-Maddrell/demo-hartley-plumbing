---
import '../styles/global.css';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="/og-image.jpg" />
    <meta property="og:type" content="website" />
    <meta name="theme-color" content="#0C1B2E" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="canonical" href={Astro.url.href} />
    <title>{title}</title>

    <!-- LocalBusiness Schema Markup -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Plumber",
      "name": "Hartley & Co Plumbing",
      "image": "/og-image.jpg",
      "url": "https://hartleyplumbing.com.au",
      "telephone": "0412 555 178",
      "email": "info@hartleyplumbing.com.au",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "78 Pitt Street",
        "addressLocality": "Sydney",
        "addressRegion": "NSW",
        "postalCode": "2000",
        "addressCountry": "AU"
      },
      "geo": {
        "@type": "GeoCoordinates",
        "latitude": -33.867,
        "longitude": 151.209
      },
      "openingHoursSpecification": [
        {
          "@type": "OpeningHoursSpecification",
          "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
          "opens": "07:00",
          "closes": "18:00"
        },
        {
          "@type": "OpeningHoursSpecification",
          "dayOfWeek": "Saturday",
          "opens": "08:00",
          "closes": "14:00"
        }
      ],
      "priceRange": "$$",
      "areaServed": {
        "@type": "GeoCircle",
        "geoMidpoint": {
          "@type": "GeoCoordinates",
          "latitude": -33.867,
          "longitude": 151.209
        },
        "geoRadius": "15000"
      },
      "aggregateRating": {
        "@type": "AggregateRating",
        "ratingValue": "4.9",
        "reviewCount": "200",
        "bestRating": "5"
      },
      "foundingDate": "2003",
      "description": "Sydney CBD's trusted plumber for 20+ years. 24/7 emergency repairs, bathroom renovations, commercial plumbing, hot water systems, and gas fitting. Licensed, insured, and guaranteed.",
      "hasOfferCatalog": {
        "@type": "OfferCatalog",
        "name": "Plumbing Services",
        "itemListElement": [
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "24/7 Emergency Plumbing" } },
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Bathroom Renovations" } },
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Commercial Plumbing" } },
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Hot Water System Installation & Repair" } },
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Drain Cleaning & CCTV Inspection" } },
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Gas Fitting" } }
        ]
      }
    })} />
  </head>
  <body class="min-h-screen flex flex-col bg-bg-cream antialiased grain-overlay">
    <Navbar />

    <!-- Emergency Alert Strip -->
    <div id="emergency-banner" class="relative bg-gradient-to-r from-copper-600 via-copper-500 to-copper-600 text-white overflow-hidden">
      <!-- Subtle animated shimmer -->
      <div class="absolute inset-0 opacity-10" style="background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent); background-size: 200% 100%; animation: shimmer 3s linear infinite;"></div>
      <div class="relative max-w-7xl mx-auto px-5 sm:px-8 lg:px-10 py-3 flex items-center justify-center gap-3 text-sm font-semibold">
        <span class="relative flex h-3 w-3 shrink-0">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-white opacity-60"></span>
          <span class="relative inline-flex rounded-full h-3 w-3 bg-white"></span>
        </span>
        <span>Emergency Plumber On Call — <a href="tel:0412555178" class="underline underline-offset-2 decoration-white/60 hover:decoration-white transition-colors">0412 555 178</a></span>
        <button id="dismiss-banner" class="absolute right-3 sm:right-6 top-1/2 -translate-y-1/2 w-7 h-7 flex items-center justify-center rounded-full hover:bg-white/20 transition-colors" aria-label="Dismiss banner">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
        </button>
      </div>
    </div>

    <main class="flex-1">
      <slot />
    </main>
    <Footer />

    <script>
      document.getElementById('dismiss-banner')?.addEventListener('click', () => {
        document.getElementById('emergency-banner')?.remove();
      });

      // Scroll-triggered reveal animations
      const revealObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('visible');
            revealObserver.unobserve(entry.target);
          }
        });
      }, { threshold: 0.08, rootMargin: '0px 0px -60px 0px' });

      document.querySelectorAll('.reveal, .reveal-left, .reveal-right').forEach(el => revealObserver.observe(el));
    </script>
    <script>
      if (new URLSearchParams(window.location.search).has('preview')) {
        document.documentElement.style.scrollBehavior = 'auto';
        document.querySelectorAll('img[loading="lazy"]').forEach(img => img.loading = 'eager');
        document.querySelectorAll('.reveal, .reveal-left, .reveal-right').forEach(el => el.classList.add('visible'));
        window.addEventListener('load', () => {
          let pos = 0, dir = 1, last = 0;
          const pxPerSec = 80;
          function autoScroll(ts) {
            if (!last) last = ts;
            const dt = ts - last;
            last = ts;
            const max = document.documentElement.scrollHeight - window.innerHeight;
            pos += dir * pxPerSec * (dt / 1000);
            if (pos >= max) { dir = -1; pos = max; }
            if (pos <= 0) { dir = 1; pos = 0; }
            window.scrollTo(0, pos);
            requestAnimationFrame(autoScroll);
          }
          requestAnimationFrame(autoScroll);
        });
      }
    </script>
  </body>
</html>
